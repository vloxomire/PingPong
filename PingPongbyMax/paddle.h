#ifndef PADDLE_H
#define PADDLE_H
#include <Figuras.h>

class Paddle : protected Figuras
{
    public:
        Paddle();
        virtual ~Paddle();

    protected:

    private:

};

#endif // PADDLE_H
